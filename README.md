# Bohan Studio

An art-directed personal website built with Vite, React, and TypeScript.

## Commands

```bash
npm install
npm run dev
npm run build
```

The production build accepts `BASE_PATH` for GitHub Pages-style subpath deploys:

```bash
BASE_PATH=/codex-personal/ npm run build
```
